# feitianduowen.github.io

个人技术主页，使用原生 HTML、CSS 和 JavaScript 构建，无需构建工具。

## 本地预览

直接用浏览器打开 `index.html`，或在仓库目录启动任意静态文件服务器：

```bash
python -m http.server 8080
```

然后访问 <http://localhost:8080>。

## 部署

将 `index.html`、`styles.css` 和 `script.js` 推送到 GitHub 仓库的 `main` 分支即可由 GitHub Pages 提供静态托管。
